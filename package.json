{
  "name": "zucchini-workspace",
  "private": true,
  "workspaces": [
    "apps/desktop",
    "apps/web"
  ],
  "scripts": {
    "preinstall": "node scripts/check-node-version.mjs",
    "dev:desktop": "pnpm --dir apps/desktop dev",
    "dev:web": "pnpm --dir apps/web dev",
    "build:desktop": "pnpm --dir apps/desktop build",
    "build:web": "pnpm --dir apps/web build",
    "test:desktop": "pnpm --dir apps/desktop test",
    "fallow": "fallow",
    "fallow:dead-code": "fallow dead-code",
    "fallow:dupes": "fallow dupes",
    "fallow:health": "fallow health --max-cyclomatic 40 --max-cognitive 30 --max-crap 80",
    "fallow:audit": "fallow audit",
    "format": "ultracite fix .",
    "lint": "ultracite check .",
    "check": "pnpm run lint && pnpm run test:desktop && pnpm run build:web"
  },
  "devDependencies": {
    "fallow": "^2.48.5",
    "oxfmt": "^0.42.0",
    "oxlint": "^1.55.0",
    "ultracite": "^7.3.0"
  },
  "engines": {
    "node": "24.x",
    "pnpm": "10.27.0"
  },
  "packageManager": "pnpm@10.27.0"
}
